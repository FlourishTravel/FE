{
  "name": "flourish-travel-fe",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "cd website && npm ci --include=dev && npm run build",
    "start": "serve website/dist -s -l tcp://0.0.0.0:${PORT:-8080}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
