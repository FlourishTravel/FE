# Included from http {} on the VPS host nginx.
# Large Vite JS/CSS were truncated at ~128KB (proxy_buffers) →
# ERR_CONTENT_LENGTH_MISMATCH 200 while Content-Length advertised the full file.
proxy_http_version 1.1;
proxy_buffering off;
proxy_request_buffering off;
proxy_max_temp_file_size 0;
