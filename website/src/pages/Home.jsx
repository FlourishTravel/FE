cd wimport React from 'react';

const Home = () => {
    return (
        <div className="min-h-screen">
            {/* This is the home page - we'll keep the existing content here */}
        </div>
    );
};

export default Home;
